version: 0.1.{build}

branches:
  only:
  - master

os: Windows Server 2012 R2

#init:
#  - git config --global core.autocrlf true

install:
- ps: Install-Product node 4.0

build_script:
- cmd: npm install

test_script:
- cmd: npm test
